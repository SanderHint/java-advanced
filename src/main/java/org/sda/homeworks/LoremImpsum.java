package org.sda.homeworks;

import java.io.File;
import java.nio.file.Files;
import java.nio.file.Paths;
import java.util.List;

/**
 * Create a file with a "Lorem ipsum" paragraph within -
 * it can be done by copy-pasting existing paragraph or generating it dynamically using Java library. Read that file.
 *
 * a) Count words.
 * b) Count special signs(like comma, dot, spaces).
 * c) Select one word and print it's number of occurences.
 *
 * @author Sander
 */

